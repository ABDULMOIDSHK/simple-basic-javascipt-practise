<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        
     tab=prompt()   
     console.log((`tab*1=${tab*1}`));  
     console.log(`tab*1=${tab*2}  `);
    console.log (`tab*1=${tab*3} `);
    console.log (`tab*1=${tab*4} `);

     console.log(`tab*1=${tab*5} `);
     console.log(`tab*1=${tab*6} `);
     console.log(`tab*1=${tab*7}`);

     console.log(`tab*1=${tab*8} `);
   console.log  (`tab*1=${tab*9}`);
   console.log (`tab*1=${tab*10} `);

    console.log (`tab*1=${tab*1}`);  
    console.log (`tab*1=${tab*2}  `);
    console.log (`tab*1=${tab*3} `);
    console.log (`tab*1=${tab*4} `);

    console.log (`tab*1=${tab*5} `);
    console.log (`tab*1=${tab*6} `);
    console.log (`tab*1=${tab*7}`);

    console.log (`tab*1=${tab*8} `);
    console.log (`tab*1=${tab*9}`);
   console.log  (`tab*1=${tab*10} `);
                    

     // a=5
    // b=5
    // document.write(prompt())



    </script>
</body>
</html>
